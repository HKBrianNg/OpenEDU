1. 安装Wangler CLI
npm install -g wrangler

2. Cloudflare 部署

a.修改代码后​，启动本地测试
    cd c:/Projects/OpenEDU/client
    npm run dev

b.本地测试后, 构建安装包
    npm run build

c.构建成功后​，上传安装包到Cloudflare平台
    npx wrangler pages deploy dist --project-name=openedu-client

d.上传后等一两分钟​ → 刷新网站查看效果
https://d24e31c9.openedu-client.pages.dev
